# Local Claude Code state (never commit local runtime, lock, or session files)
.claude/agent-memory/
.claude/*.lock
.claude/settings.local.json

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Local test artifacts
.tmp/
*.log

# Local Codex project-rules artifact generated by
# plugins/krylo/scripts/setup/install-codex.mjs --target rules against this
# repository itself (dogfooding); never a source-controlled product file.
.codex/

# Local checkpoint/review artifacts (task input, generated diffs/status for
# independent review) -- never product files, never shipped, never committed.
# See docs/process/FILE_MANIFEST.md and RELEASE_READINESS.md.
prompt.md
*.patch
*-commits.txt
*-status.txt
