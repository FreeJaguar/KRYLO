# KRYLO Claude Host Capability Matrix

- Checkpoint: `docs/adr/0030-cross-harness-advisory-workers.md`, `docs/process/CROSS_HARNESS_IMPLEMENTATION_PLAN.md`
- Installed Claude CLI verified against: `2.1.197 (Claude Code)`
- Scope: this is the first Claude-side capability matrix this product has needed -- Claude host capabilities before Cross-Harness were documented inline in ADRs (0025/0027) rather than in a dedicated matrix. It currently covers only the capabilities Cross-Harness introduced (invoking `claude -p` as an opposite-provider advisory worker); it is not a restatement of the whole Claude host.
- Evidence categories, matching `docs/codex-capability-matrix.md`'s own convention: **locally passed** (real command executed, real output observed), **statically inspected** (source/config read and reasoned about, not executed), **blocked by platform** (this environment cannot exercise it -- no authenticated live Cross-Harness invocation).

| Capability | Status | Evidence | Safe fallback |
|---|---|---|---|
| `claude` CLI capability detection | Locally passed | `detectClaudeWorkerCapability()` resolves the real installed `claude` binary and reports `2.1.197 (Claude Code)`; confirmed both via direct test and via the real Cross-Harness end-to-end suite | Unavailable/unsupported-version reports `WORKER_UNAVAILABLE`/`WORKER_UNSUPPORTED_VERSION`, Cross-Harness degrades to native-only verification |
| Windows executable resolution (`spawnSync('claude', ...)`) | **Confirmed broken on the naive approach, fixed and locally passed** | Direct reproduction: `spawnSync('claude', [...], {shell:false})` fails with `EINVAL` on this platform (npm-installed CLIs are `.cmd` shims on Windows, which Node refuses to exec without a shell). The naive fix, `shell:true`, was directly reproduced as UNSAFE (an argv element containing `&&` was genuinely executed as a second shell command; Node's own `DEP0190` deprecation warning confirms this generally). Fixed with `scripts/lib/spawn-platform.mjs` (`cmd.exe /d /s /c <command> <args>`, `shell:false`, argv array intact) -- verified functionally (`claude --version`/`codex --version` both resolve correctly) and for injection-safety (a metacharacter-laden argv element passes through as inert literal text, confirmed against real `node.exe`/`cmd.exe`) | N/A -- this is the shipped, tested mechanism, not a fallback |
| Read-only worker enforcement (`--tools Read,Grep,Glob`, `--permission-mode plan`, `--strict-mcp-config`, `--bare`) | Statically inspected + unit-tested argv construction | `buildClaudeWorkerArgv()` never includes a write-capable or MCP-capable tool name; confirmed by `tests/security/cross-harness-process.test.mjs`. A live disposable read-only probe (task Section 26 -- attempt to create `SHOULD_NOT_EXIST.txt` via a real authenticated `claude -p` invocation) was not exercised in this environment | Live probe requires an authenticated session; recorded as blocked-by-platform, not claimed as verified. The fake-worker fixture's own write-attempt mode confirms the probe TECHNIQUE is sound (`tests/hooks/cross-harness-run.test.mjs`) |
| Process spawning (argv array, `shell:false`, stdin payload, bounded timeout/output, orphaned-process cleanup) | Locally passed | `tests/unit/spawn-platform.test.mjs`, `tests/hooks/cross-harness-run.test.mjs`: real timeout, real process-tree kill via `taskkill /t` (found and fixed a real orphaned-grandchild-process leak on Windows -- `spawnSync`'s own timeout only reaches the direct `cmd.exe` wrapper, never the grandchild worker), real cleanup of the disposable invocation directory | N/A |
| Output parsing (`--output-format json`) | Locally passed against the fake-worker fixture's real Claude-shaped output wrapper | `parseClaudeWorkerOutput()` tested end to end via the fixture; malformed/oversized/malicious output all correctly rejected as `INVALID_OUTPUT`, never partially trusted | N/A |
| Environment governance (explicit allowlist, no secret passthrough, recursion-prevention markers) | Locally passed | `tests/security/cross-harness-process.test.mjs` confirms `AWS_SECRET_ACCESS_KEY`/`GITHUB_TOKEN`/`DATABASE_URL`/an arbitrary var never reach the child; `KRYLO_EXTERNAL_WORKER`/`KRYLO_PARENT_RUN_ID`/`KRYLO_DELEGATION_DEPTH` always present | N/A |
| Live authenticated Cross-Harness invocation (Claude native -> Codex worker, or vice versa) | **Blocked by platform** | No authenticated live-session smoke test was run in this environment (task Section 25 explicitly allows marking this environment-blocked rather than claiming it as a platform limitation) | Process-level fixture tests (12 end-to-end CLI tests against the real spawn/timeout/cleanup path with deterministic fake workers) are the evidence of record instead |

## Documented follow-up

- **Live read-only capability probe (task Section 26)** against a real authenticated `claude -p` and `codex exec` session was not exercised in this environment. The probe technique itself (prompt a worker to create `SHOULD_NOT_EXIST.txt`, assert absence afterward) is implemented and proven sound against the fake-worker fixture; running it against the real CLIs requires authenticated credentials not available here.
- **Codex-native -> Claude-worker direction cannot obtain real human approval today**, since it inherits ADR-0029's already-shipped deterministic-deny boundary for every `require-approval` class on Codex. This is a disclosed, capability-driven asymmetry identical in kind to the existing Codex `require-approval` asymmetry (`docs/codex-capability-matrix.md`), not a new gap -- see `docs/adr/0030-cross-harness-advisory-workers.md`'s Consequences.
